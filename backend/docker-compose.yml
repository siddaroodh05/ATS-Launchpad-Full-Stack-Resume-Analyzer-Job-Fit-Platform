services:
  backend:
    build: .
    image: atslaunchpad-backend:latest
    container_name: atslaunchpad-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"